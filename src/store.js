/*
file nel quale ci sarà il BASE_URL

importare reactive from vue

inizializzare una variabile store nel quale ci sarà il BASE_URL che punterà alla rotta delle API

const store = reactive({
    BASE_URL: 'http://localhost:3000/api'
});

esportare la variabile store
*/